---
const { categories } = Astro.props as { categories: string[] };
---

<div class="flex flex-wrap gap-2">
  {categories.map((category) => (
    <a
      href={`/categories/${encodeURIComponent(category)}`}
      class="tag"
    >
      {category}
    </a>
  ))}
</div>
